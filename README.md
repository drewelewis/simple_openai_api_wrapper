# Azure FastAPI wrapper over Azure OpenAI

Location for code required to demo FastAPI wrapper over Azure OpenAI

# Getting Started

* First set virtual environment running _env_creeate.bat
* Activate the virtual envirnonment using _env_activate.bat
* Then, at the root of your project, run _install.bat to install dependencies
* Start the server with _run_server.bat

